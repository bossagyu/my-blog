<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Vector Search on Bossagyu Blog</title><link>https://bossagyu.com/en/tags/vector-search/</link><description>Recent content in Vector Search on Bossagyu Blog</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Thu, 11 Jun 2026 00:00:00 +0900</lastBuildDate><atom:link href="https://bossagyu.com/en/tags/vector-search/index.xml" rel="self" type="application/rss+xml"/><item><title>Milvus Architecture Explained: Understanding the Four-Layer Design and Data Flows with Diagrams</title><link>https://bossagyu.com/en/blog/052-milvus-architecture/</link><pubDate>Thu, 11 Jun 2026 00:00:00 +0900</pubDate><guid>https://bossagyu.com/en/blog/052-milvus-architecture/</guid><description>&lt;h2 id="overview">Overview&lt;/h2>
&lt;p>With the rise of RAG (Retrieval-Augmented Generation) and recommendation systems, more and more teams are adopting &lt;strong>Milvus&lt;/strong>, one of the most popular vector databases. Getting started with Milvus through its SDK is easy, but once you reach production operations and tuning, understanding how data actually flows inside the system makes a huge difference.&lt;/p>
&lt;p>In this article, I explain the Milvus architecture based on the official documentation, with diagrams to make it easy to follow. You can also read it as a follow-up to my previous article, &amp;ldquo;&lt;a class="link" href="https://bossagyu.com/en/blog/051-ann-basics/" >Introduction to Approximate Nearest Neighbor (ANN) Search&lt;/a>&amp;rdquo;, showing how those search algorithms operate as a distributed system.&lt;/p>
&lt;p>Note that this article is based on the official documentation for &lt;strong>Milvus 2.6&lt;/strong> (&lt;a class="link" href="https://milvus.io/docs/architecture_overview.md" target="_blank" rel="noopener"
>Milvus Architecture Overview&lt;/a>). The architecture was significantly reorganized in 2.6, so the component layout differs from earlier versions.&lt;/p>
&lt;h2 id="what-is-milvus">What Is Milvus?&lt;/h2>
&lt;p>Milvus is an &lt;strong>open-source, cloud-native vector database&lt;/strong> designed for high-performance similarity search. Internally it leverages vector search libraries such as Faiss, HNSW, DiskANN, and SCANN, and it is widely used as a search backbone for AI applications and unstructured data (source: &lt;a class="link" href="https://milvus.io/docs/architecture_overview.md" target="_blank" rel="noopener"
>Milvus Architecture Overview&lt;/a>).&lt;/p>
&lt;p>Two design principles define its architecture:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Disaggregation of storage and computing&lt;/strong>: all components are stateless, and the actual data lives in external storage&lt;/li>
&lt;li>&lt;strong>Separation of the data plane and control plane&lt;/strong>: the components that process data and the components that manage the cluster are independent, so each can scale horizontally&lt;/li>
&lt;/ul>
&lt;h2 id="the-four-layer-architecture-at-a-glance">The Four-Layer Architecture at a Glance&lt;/h2>
&lt;p>A Milvus cluster consists of four layers (source: &lt;a class="link" href="https://milvus.io/docs/four_layers.md" target="_blank" rel="noopener"
>Storage/Computing Disaggregation&lt;/a>).&lt;/p>
&lt;p>&lt;img src="https://bossagyu.com/en/blog/052-milvus-architecture/img-052-001-en.svg"
loading="lazy"
alt="The four-layer Milvus architecture: access layer (Proxy), control plane (Coordinator), worker nodes (Streaming/Query/Data Node), and storage layer (WAL/Object/Meta Storage)"
>&lt;/p>
&lt;h3 id="access-layer">Access Layer&lt;/h3>
&lt;p>The access layer is a set of &lt;strong>stateless proxies&lt;/strong> that receive client requests first. Their responsibilities are:&lt;/p>
&lt;ul>
&lt;li>Validating requests&lt;/li>
&lt;li>Providing a unified service endpoint together with load balancers such as Nginx or Kubernetes Ingress&lt;/li>
&lt;li>Aggregating intermediate results from worker nodes and returning the final result to the client&lt;/li>
&lt;/ul>
&lt;p>Because Milvus follows a massively parallel processing (MPP) design, it is the proxy that consolidates partial results into the final answer.&lt;/p>
&lt;h3 id="control-plane-coordinator">Control Plane (Coordinator)&lt;/h3>
&lt;p>The Coordinator is described in the official documentation as &amp;ldquo;&lt;strong>the brain of Milvus&lt;/strong>&amp;rdquo;. It maintains cluster topology, schedules work, and ensures consistency. Its concrete responsibilities include:&lt;/p>
&lt;ul>
&lt;li>Handling DDL/DCL operations (creating collections, managing permissions, and so on)&lt;/li>
&lt;li>Timestamp management via the &lt;strong>TSO (Timestamp Oracle)&lt;/strong>&lt;/li>
&lt;li>Managing WAL bindings with Streaming Nodes&lt;/li>
&lt;li>Managing Query Node topology and load balancing&lt;/li>
&lt;li>Distributing offline tasks such as compaction and index building&lt;/li>
&lt;/ul>
&lt;p>Exactly one Coordinator is active in the cluster, and a master-slave mode can be enabled for high availability (source: &lt;a class="link" href="https://milvus.io/docs/main_components.md" target="_blank" rel="noopener"
>Milvus Main Components&lt;/a>).&lt;/p>
&lt;h3 id="worker-nodes-execution-layer">Worker Nodes (Execution Layer)&lt;/h3>
&lt;p>Three types of stateless nodes execute the Coordinator&amp;rsquo;s instructions. The table below summarizes their roles.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Node&lt;/th>
&lt;th>Main role&lt;/th>
&lt;th>Data handled&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>Streaming Node&lt;/td>
&lt;td>Real-time writes, shard-level consistency and recovery via the WAL, search on incremental data, converting growing data into sealed segments&lt;/td>
&lt;td>Growing segments (freshly written data)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Query Node&lt;/td>
&lt;td>Loading historical data from object storage and searching it&lt;/td>
&lt;td>Sealed segments (persisted data)&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Data Node&lt;/td>
&lt;td>Offline processing such as compaction and index building&lt;/td>
&lt;td>Sealed segments&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>In Milvus 2.6, the roles were cleanly split — stream processing goes to the Streaming Node, batch processing goes to the Query Node and Data Node — achieving both real-time performance and high throughput (source: &lt;a class="link" href="https://milvus.io/blog/introduce-milvus-2-6-built-for-scale-designed-to-reduce-costs.md" target="_blank" rel="noopener"
>Introducing Milvus 2.6&lt;/a>).&lt;/p>
&lt;h3 id="storage-layer">Storage Layer&lt;/h3>
&lt;p>The storage layer holds the actual data and consists of three kinds of storage.&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Storage&lt;/th>
&lt;th>Role&lt;/th>
&lt;th>Implementations&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>Meta Storage&lt;/td>
&lt;td>Metadata snapshots, service registration&lt;/td>
&lt;td>etcd&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>WAL Storage&lt;/td>
&lt;td>Durability of writes (write-ahead logging)&lt;/td>
&lt;td>Woodpecker, Kafka, Pulsar&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>Object Storage&lt;/td>
&lt;td>Segments (log snapshots) and index files&lt;/td>
&lt;td>MinIO, AWS S3, Azure Blob&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The reason every worker node can stay stateless is that all state is pushed down into this layer. Even if a node crashes, data can be recovered from the WAL and object storage.&lt;/p>
&lt;h2 id="the-write-path">The Write Path&lt;/h2>
&lt;p>Let&amp;rsquo;s look at how an insert request is processed (source: &lt;a class="link" href="https://milvus.io/docs/data_processing.md" target="_blank" rel="noopener"
>Data Processing in Milvus&lt;/a>).&lt;/p>
&lt;p>&lt;img src="https://bossagyu.com/en/blog/052-milvus-architecture/img-052-002-en.svg"
loading="lazy"
alt="The Milvus write path: the proxy splits data by shard, the Streaming Node assigns a TSO and persists the data to the WAL, and data moves from growing segments to sealed segments in object storage"
>&lt;/p>
&lt;ol>
&lt;li>&lt;strong>The proxy routes data to shards&lt;/strong>: a collection is divided into multiple &lt;strong>shards&lt;/strong>, and each shard maps to a &lt;strong>vchannel (virtual channel)&lt;/strong>. The proxy splits incoming data into per-shard packages according to the shard routing rules&lt;/li>
&lt;li>&lt;strong>The Streaming Node assigns a TSO&lt;/strong>: the Streaming Node responsible for each vchannel assigns a timestamp (TSO) to guarantee operation ordering and validates consistency&lt;/li>
&lt;li>&lt;strong>Write to the WAL&lt;/strong>: data is appended to WAL storage, and &lt;strong>the write is considered successful once it is durable there&lt;/strong>. After a crash, the Streaming Node can replay the WAL to fully recover all pending operations&lt;/li>
&lt;li>&lt;strong>Applied to a growing segment&lt;/strong>: the Streaming Node asynchronously converts WAL entries into segments. Freshly written data becomes an in-memory &lt;strong>growing segment&lt;/strong> and is already searchable at this point&lt;/li>
&lt;li>&lt;strong>Flush turns it into a sealed segment&lt;/strong>: when a flush is triggered — for example, once the segment reaches its capacity threshold — the growing segment becomes a &lt;strong>sealed segment&lt;/strong> — immutable and persisted — in object storage&lt;/li>
&lt;li>&lt;strong>The Data Node builds indexes&lt;/strong>: the Data Node builds an index for each sealed segment independently and stores the result back in object storage&lt;/li>
&lt;/ol>
&lt;p>Because vector index building is computationally demanding, it relies on &lt;strong>SIMD acceleration&lt;/strong> with instruction sets such as SSE, AVX2, and AVX512. Scalar fields use structures like Bloom filters and inverted indexes.&lt;/p>
&lt;h2 id="the-search-path">The Search Path&lt;/h2>
&lt;p>A search request flows in the opposite direction: it gathers data that is spread across the cluster.&lt;/p>
&lt;p>&lt;img src="https://bossagyu.com/en/blog/052-milvus-architecture/img-052-003-en.svg"
loading="lazy"
alt="The Milvus search path: the proxy broadcasts to the Streaming Nodes of all shards, each Streaming Node searches growing segments and delegates sealed-segment search to Query Nodes, and the proxy merges the per-shard results"
>&lt;/p>
&lt;ol>
&lt;li>&lt;strong>The proxy broadcasts to all shards&lt;/strong>: the search request is sent concurrently to every Streaming Node responsible for the related shards&lt;/li>
&lt;li>&lt;strong>Streaming Nodes search the freshest data&lt;/strong>: each Streaming Node generates a query plan and searches its local growing segments (data written moments ago)&lt;/li>
&lt;li>&lt;strong>Query Nodes search historical data&lt;/strong>: each Streaming Node also delegates the search over sealed segments (historical data) to remote Query Nodes, which search using segments and indexes loaded in advance from object storage&lt;/li>
&lt;li>&lt;strong>The proxy merges the results&lt;/strong>: the proxy collects the results from all shards, merges them, and returns the final outcome to the client&lt;/li>
&lt;/ol>
&lt;p>This design lets Milvus &lt;strong>return just-written data in search results while still searching massive historical data quickly through indexes&lt;/strong>. When growing segments are flushed into sealed segments, the Coordinator performs a &lt;strong>handoff&lt;/strong>, distributing the sealed segments evenly across the Query Nodes.&lt;/p>
&lt;h2 id="deployment-modes-standalone-vs-cluster">Deployment Modes: Standalone vs. Cluster&lt;/h2>
&lt;p>Milvus offers two main deployment modes (source: &lt;a class="link" href="https://milvus.io/docs/main_components.md" target="_blank" rel="noopener"
>Milvus Main Components&lt;/a>).&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Standalone mode&lt;/strong>: all components run in a single process. It suits small datasets and low workloads, and it can use embedded WAL implementations such as Woodpecker or rocksmq, eliminating third-party middleware dependencies. Note that &lt;strong>you cannot perform an online upgrade from a standalone instance to a cluster&lt;/strong>&lt;/li>
&lt;li>&lt;strong>Cluster mode&lt;/strong>: each component runs as an independent process and can be scaled out individually on Kubernetes. This mode is for large datasets and high-load production environments&lt;/li>
&lt;/ul>
&lt;p>A typical setup is standalone for development and testing, and cluster mode for production.&lt;/p>
&lt;h2 id="how-the-architecture-evolved-in-milvus-26">How the Architecture Evolved in Milvus 2.6&lt;/h2>
&lt;p>Finally, here are the major architectural changes introduced in Milvus 2.6 (source: &lt;a class="link" href="https://milvus.io/blog/introduce-milvus-2-6-built-for-scale-designed-to-reduce-costs.md" target="_blank" rel="noopener"
>Introducing Milvus 2.6&lt;/a>).&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Introduction of the Streaming Node&lt;/strong>: real-time responsibilities that were scattered across multiple components in 2.5 — consuming the message queue, writing incremental segments, serving incremental queries, and WAL-based recovery — are now consolidated into a dedicated Streaming Node&lt;/li>
&lt;li>&lt;strong>Woodpecker (zero-disk WAL)&lt;/strong>: a cloud-native WAL system built to remove the dependency on external message queues like Kafka and Pulsar. With its &amp;ldquo;zero-disk&amp;rdquo; design, all log data is persisted directly in object storage, with no reliance on local disks (source: &lt;a class="link" href="https://milvus.io/docs/woodpecker_architecture.md" target="_blank" rel="noopener"
>Woodpecker Architecture&lt;/a>)&lt;/li>
&lt;li>&lt;strong>Coordinator merge&lt;/strong>: the previously separate coordinators (RootCoord, QueryCoord, DataCoord, and so on) were merged into a single Coordinator, simplifying operations&lt;/li>
&lt;/ul>
&lt;p>All of these changes move in the same direction: fewer external dependencies and a simpler, cloud-native architecture that reduces cost and operational burden.&lt;/p>
&lt;h2 id="summary">Summary&lt;/h2>
&lt;p>In this article, I walked through the Milvus architecture based on the official documentation.&lt;/p>
&lt;ul>
&lt;li>Milvus is a cloud-native design built on &lt;strong>storage/computing disaggregation&lt;/strong>, consisting of four layers: the access layer (Proxy), the control plane (Coordinator), worker nodes, and the storage layer&lt;/li>
&lt;li>The worker nodes have clearly separated roles: &lt;strong>the Streaming Node handles real-time processing, the Query Node searches historical data, and the Data Node handles offline processing&lt;/strong>&lt;/li>
&lt;li>Writes are processed in stages — committed once durable in the WAL, then growing segment, then sealed segment via flush — while searches merge results from fresh and historical data at the proxy&lt;/li>
&lt;li>Milvus 2.6 introduced the Streaming Node and Woodpecker (zero-disk WAL) and merged the coordinators, evolving toward a simpler architecture with fewer external dependencies&lt;/li>
&lt;/ul>
&lt;p>Understanding these internal data flows makes it clear why just-written data shows up in search results and why data survives node failures — knowledge that pays off in capacity planning and troubleshooting in production.&lt;/p>
&lt;h2 id="references">References&lt;/h2>
&lt;ul>
&lt;li>&lt;a class="link" href="https://milvus.io/docs/architecture_overview.md" target="_blank" rel="noopener"
>Milvus Architecture Overview | Milvus Documentation&lt;/a>&lt;/li>
&lt;li>&lt;a class="link" href="https://milvus.io/docs/four_layers.md" target="_blank" rel="noopener"
>Storage/Computing Disaggregation | Milvus Documentation&lt;/a>&lt;/li>
&lt;li>&lt;a class="link" href="https://milvus.io/docs/main_components.md" target="_blank" rel="noopener"
>Main Components | Milvus Documentation&lt;/a>&lt;/li>
&lt;li>&lt;a class="link" href="https://milvus.io/docs/data_processing.md" target="_blank" rel="noopener"
>Data Processing | Milvus Documentation&lt;/a>&lt;/li>
&lt;li>&lt;a class="link" href="https://milvus.io/blog/introduce-milvus-2-6-built-for-scale-designed-to-reduce-costs.md" target="_blank" rel="noopener"
>Introducing Milvus 2.6: Affordable Vector Search at Billion Scale | Milvus Blog&lt;/a>&lt;/li>
&lt;li>&lt;a class="link" href="https://milvus.io/docs/woodpecker_architecture.md" target="_blank" rel="noopener"
>Woodpecker Architecture | Milvus Documentation&lt;/a>&lt;/li>
&lt;/ul></description></item><item><title>Introduction to Approximate Nearest Neighbor (ANN) Search: How Vector Search Works, Explained for Beginners</title><link>https://bossagyu.com/en/blog/051-ann-basics/</link><pubDate>Tue, 09 Jun 2026 00:00:00 +0900</pubDate><guid>https://bossagyu.com/en/blog/051-ann-basics/</guid><description>&lt;h2 id="overview">Overview&lt;/h2>
&lt;p>RAG (Retrieval-Augmented Generation) systems that combine LLMs like ChatGPT with internal documents, &amp;ldquo;recommended for you&amp;rdquo; sections on e-commerce sites, similar-image search — behind all of these runs a technology called &lt;strong>vector search&lt;/strong>. And what makes vector search fast enough for real-world use is the topic of this article: &lt;strong>ANN (Approximate Nearest Neighbor) search&lt;/strong>.&lt;/p>
&lt;p>In this article, I will explain what ANN is, why &amp;ldquo;approximate&amp;rdquo; is good enough, and walk through the six building blocks of an ANN system — indexes, quantization, distance metrics, search parameters, storage strategy, and pre/post-processing — in a way that beginners can follow.&lt;/p>
&lt;h2 id="what-is-ann">What Is ANN?&lt;/h2>
&lt;h3 id="starting-with-nearest-neighbor-search">Starting with Nearest Neighbor Search&lt;/h3>
&lt;p>Nearest neighbor search is the task of finding the data point closest to a given query point among a large collection of data.&lt;/p>
&lt;p>In the context of vector search, documents and images are converted in advance into &lt;strong>embeddings&lt;/strong> — arrays of numbers with hundreds to thousands of dimensions. Texts with similar meanings end up close to each other in this vector space. In other words, &amp;ldquo;finding semantically similar documents&amp;rdquo; can be reformulated as &amp;ldquo;finding nearby vectors in space.&amp;rdquo;&lt;/p>
&lt;p>The most naive approach is to compare the query vector against every single vector in the dataset, one by one. This is brute-force &lt;strong>kNN (k-Nearest Neighbors)&lt;/strong> search. It always returns the exact answer, but with 100 million records you need 100 million distance computations — far too slow for real-time search.&lt;/p>
&lt;h3 id="trading-exactness-for-speed">Trading Exactness for Speed&lt;/h3>
&lt;p>This is where ANN comes in. ANN takes the approach of &amp;ldquo;&lt;strong>giving up the guarantee of a 100% exact answer in exchange for returning a nearly correct answer orders of magnitude faster&lt;/strong>.&amp;rdquo;&lt;/p>
&lt;p>Imagine looking for a book in a library. If you scan every shelf from end to end, you will definitely find the book — after several hours. In practice, you rely on the classification system: &amp;ldquo;cookbooks should be around here,&amp;rdquo; and only check the shelves that look relevant. Occasionally you might miss a book that was shelved somewhere unexpected, but most of the time you find what you want in minutes. ANN turns this &amp;ldquo;narrow down first, then look&amp;rdquo; strategy into a data structure.&lt;/p>
&lt;p>&lt;img src="https://bossagyu.com/en/blog/051-ann-basics/img-051-001-en.svg"
loading="lazy"
alt="Brute-force kNN vs ANN: brute force computes the distance to every point, while ANN searches only the regions likely to be close"
>&lt;/p>
&lt;p>Search quality is measured by &lt;strong>recall&lt;/strong>. For example, if you return 9 of the true top 10 results, your recall is 90%. In practice, the sweet spot is typically maintaining 95–99% recall while running hundreds to thousands of times faster than brute force.&lt;/p>
&lt;h2 id="the-six-building-blocks-of-an-ann-system">The Six Building Blocks of an ANN System&lt;/h2>
&lt;p>ANN is not a single algorithm but a combination of several techniques. Let&amp;rsquo;s look at the six main components one by one.&lt;/p>
&lt;h3 id="1-indexes-search-data-structures">1. Indexes (Search Data Structures)&lt;/h3>
&lt;p>At the heart of ANN is the index — a data structure built for fast search. Indexes fall into three major families.&lt;/p>
&lt;p>&lt;strong>Graph-based&lt;/strong> methods build a graph connecting nearby vectors to each other, then traverse the edges to home in on the neighbors. The most prominent example is &lt;strong>HNSW (Hierarchical Navigable Small World)&lt;/strong>, currently the most widely used index. Like a highway network, it maintains both coarse long-distance links and fine-grained local links in a hierarchical structure. Other examples include NSG and Vamana (used in DiskANN).&lt;/p>
&lt;p>&lt;img src="https://bossagyu.com/en/blog/051-ann-basics/img-051-002-en.svg"
loading="lazy"
alt="HNSW&amp;rsquo;s hierarchical structure: approach roughly on the upper layers, then descend to search precisely on the bottom layer"
>&lt;/p>
&lt;p>A search starts from the entry point on the sparse top layer. On each layer, it moves as close to the query as it can, then descends to the layer below and repeats. The &amp;ldquo;long-distance jumps&amp;rdquo; on the upper layers carry you near the destination quickly, and the fine-grained search on the bottom layer finishes the job — reaching the neighbors with very few distance computations.&lt;/p>
&lt;p>&lt;strong>Tree-based&lt;/strong> methods recursively partition the space using tree structures. Classic examples are &lt;strong>KD-tree&lt;/strong> and &lt;strong>Annoy&lt;/strong>, developed by Spotify.&lt;/p>
&lt;p>&lt;strong>Hash-based&lt;/strong> methods use special hash functions designed so that nearby vectors are likely to receive the same hash value. The representative technique is &lt;strong>LSH (Locality Sensitive Hashing)&lt;/strong>.&lt;/p>
&lt;p>In addition, &lt;strong>IVF (Inverted File Index)&lt;/strong> is another popular approach: it clusters the data ahead of time and searches only the clusters closest to the query.&lt;/p>
&lt;h3 id="2-quantization">2. Quantization&lt;/h3>
&lt;p>Quantization compresses vectors to reduce memory usage and speed up distance computations. It is used in combination with indexes.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>SQ (Scalar Quantization)&lt;/strong>: converts each dimension from float32 to int8 or similar, compressing the data to roughly 1/4 the size&lt;/li>
&lt;li>&lt;strong>PQ (Product Quantization)&lt;/strong>: splits each vector into multiple subspaces and encodes each part as the ID of a representative point (a code). It achieves high compression ratios and is a staple for large-scale data&lt;/li>
&lt;li>&lt;strong>BQ (Binary Quantization)&lt;/strong>: compresses each dimension down to a single bit, enabling ultra-fast distance computation with bitwise operations&lt;/li>
&lt;/ul>
&lt;p>The idea behind PQ looks like this: split the vector into parts and replace each part with the ID of a representative point, drastically reducing the data size.&lt;/p>
&lt;p>&lt;img src="https://bossagyu.com/en/blog/051-ann-basics/img-051-003-en.svg"
loading="lazy"
alt="How PQ works: split the vector into subvectors and replace each part with a representative code, compressing 512 bytes down to 4 bytes"
>&lt;/p>
&lt;p>The more you compress, the more memory you save — but accuracy drops because information is lost. Here too, you are tuning a trade-off.&lt;/p>
&lt;h3 id="3-distance-metrics">3. Distance Metrics&lt;/h3>
&lt;p>How you define &amp;ldquo;near&amp;rdquo; is another important choice. The main distance (similarity) metrics are:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Cosine similarity&lt;/strong>: measures similarity by the angle between vectors. Commonly used for text embeddings&lt;/li>
&lt;li>&lt;strong>Euclidean distance (L2)&lt;/strong>: the straight-line distance in space&lt;/li>
&lt;li>&lt;strong>Inner product (dot product)&lt;/strong>: reflects both the direction and the magnitude of vectors&lt;/li>
&lt;/ul>
&lt;p>Which one to choose depends on the characteristics of the model that produced the embeddings. Most embedding models document their recommended metric, and following that recommendation is the standard practice.&lt;/p>
&lt;h3 id="4-search-parameters-tuning">4. Search Parameters (Tuning)&lt;/h3>
&lt;p>ANN indexes expose &amp;ldquo;knobs&amp;rdquo; for adjusting the trade-off between accuracy and speed.&lt;/p>
&lt;p>For HNSW:&lt;/p>
&lt;ul>
&lt;li>&lt;code>M&lt;/code>: the number of connections (edges) per node. Larger values improve accuracy but increase memory usage&lt;/li>
&lt;li>&lt;code>ef_construction&lt;/code>: the number of candidates examined during index construction. Larger values produce a better graph but take longer to build&lt;/li>
&lt;li>&lt;code>ef_search&lt;/code>: the number of candidates kept during search. Larger values improve recall but slow down queries&lt;/li>
&lt;/ul>
&lt;p>For IVF:&lt;/p>
&lt;ul>
&lt;li>&lt;code>nprobe&lt;/code>: the number of clusters searched per query. Larger values improve accuracy but reduce speed&lt;/li>
&lt;/ul>
&lt;p>In real deployments, the standard approach is to measure recall and latency on your own data and find the smallest parameter values that meet your requirements.&lt;/p>
&lt;h3 id="5-storage-strategy">5. Storage Strategy&lt;/h3>
&lt;p>Where you keep the data is another major design decision.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>In-memory&lt;/strong>: keep everything in RAM. Fastest, but memory costs balloon at the scale of hundreds of millions of records&lt;/li>
&lt;li>&lt;strong>Disk-based&lt;/strong>: approaches like &lt;strong>DiskANN&lt;/strong> keep most of the index on SSD. Somewhat slower, but they make datasets too large for memory affordable to handle&lt;/li>
&lt;/ul>
&lt;h3 id="6-pre-processing-and-post-processing">6. Pre-processing and Post-processing&lt;/h3>
&lt;p>There are also techniques outside the index itself that improve accuracy and cost.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Dimensionality reduction (pre-processing)&lt;/strong>: techniques like PCA reduce the number of dimensions, lowering compute and memory requirements&lt;/li>
&lt;li>&lt;strong>Reranking (post-processing)&lt;/strong>: after ANN coarsely narrows down the candidates (say, to the top 100), only those candidates are re-sorted using the uncompressed vectors or exact distance computation. This is a key technique for recovering the accuracy lost to quantization at a small additional cost&lt;/li>
&lt;/ul>
&lt;h2 id="designing-practical-systems">Designing Practical Systems&lt;/h2>
&lt;p>Combining all the elements above to balance &lt;strong>speed, accuracy, memory, and cost&lt;/strong> is the essence of building a practical ANN system.&lt;/p>
&lt;p>A particularly common pattern is &lt;strong>quantization + index + reranking&lt;/strong>:&lt;/p>
&lt;ol>
&lt;li>Load PQ-compressed vectors into an HNSW or IVF index to narrow down candidates quickly with a small memory footprint&lt;/li>
&lt;li>Re-sort only the small set of remaining candidates using the original full-precision vectors&lt;/li>
&lt;/ol>
&lt;div class="mermaid-wrapper" onclick="openMermaidModal(this)">
&lt;pre class="mermaid">flowchart LR
A[Query vector] --> B["Coarse filtering with ANN index&lt;br/>(HNSW / IVF + quantization)"]
B -->|"top 100 candidates"| C["Reranking&lt;br/>(exact recompute with original vectors)"]
C -->|"top 10"| D[Final results]
&lt;/pre>
&lt;span class="mermaid-hint">クリックで拡大&lt;/span>
&lt;/div>
&lt;p>This two-stage design gives you the best of both worlds: memory is saved through compression, while final accuracy is guaranteed by reranking. It appears constantly in large-scale vector search systems, so it is well worth remembering.&lt;/p>
&lt;h2 id="summary">Summary&lt;/h2>
&lt;p>In this article, I covered the fundamentals of ANN (Approximate Nearest Neighbor) search.&lt;/p>
&lt;ul>
&lt;li>ANN trades a small amount of exactness for orders-of-magnitude speedups, and it underpins vector search applications such as RAG and recommendations&lt;/li>
&lt;li>An ANN system is a combination of multiple components: indexes (HNSW, IVF, etc.), quantization (PQ, SQ, BQ), distance metrics, search parameters, storage strategy, and pre/post-processing&lt;/li>
&lt;li>The essence of the design is tuning the speed–accuracy–memory–cost trade-off, with quantization + index + reranking as the go-to pattern&lt;/li>
&lt;/ul>
&lt;p>Libraries and databases such as Faiss, Milvus, and pgvector let you use these mechanisms without implementing them yourself. However, understanding what the parameters mean and how the trade-offs are structured makes a big difference in how well you can tune a system in production. I hope this article serves as that foundation.&lt;/p></description></item></channel></rss>